spring.application.name=careersync-backend

spring.datasource.url=jdbc:mysql://localhost:3306/careersync
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

gemini.api.key=
# --- Mail (used for OTP emails) ---
# If host is left empty, the OTP is printed in the backend console instead of emailed.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=
spring.mail.password=mail_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- OTP settings ---
app.otp.expiry-minutes=5
app.otp.length=6

# --- Session (session-based authentication) ---
server.servlet.session.timeout=30m
